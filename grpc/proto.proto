syntax = "proto3";
option go_package = "MandatoryActivityFour/grpc/proto";

message Request {
  int64 lamportTime = 1;
  int64 nid = 2;
}

message Response {
  bool grant = 1;
}

service Maf {
  rpc NodeRequest(Request) returns (Response);
}